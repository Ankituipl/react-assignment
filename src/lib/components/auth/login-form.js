import React from 'react'

const LoginForm = () => {
  return (
    <div>
    {/* <form onSubmit={handleSubmit}>
        <div className="input-container">
            <label htmlFor="email">Email </label>
            <input type="email" className="input-control" placeholder="Your Email" name="email"
                   value={values.email} onChange={handleChange}/>
            {errors.email && <p className='error'>{errors.email}</p>}
        </div>
        <div className="input-container">
            <label htmlFor="password">Password </label>
            <input type="password" className="input-control" placeholder="Enter Password" name="password"
                   value={values.password} onChange={handleChange}/>
            {errors.password && <p className='error'>{errors.password}</p>}
        </div>
        <div className="input-container text-right">
            <Link to={"/forgot-password"} className="textLink">Forgot password?</Link>
        </div>
        <div>
            <button type="submit" className="login-btn">Submit</button>
        </div>
    </form> */}
</div>
  )
}

export default LoginForm
